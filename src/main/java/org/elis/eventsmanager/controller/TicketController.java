package org.elis.eventsmanager.controller;

import jakarta.validation.Valid;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import static org.elis.eventsmanager.util.UtilPath.CREATE_TICKET_VENDOR;

@RestController
public class TicketController {

  /*  @PostMapping(CREATE_TICKET_VENDOR)
    public ResponseEntity<Void> createTicket(@Valid @RequestBody CreateTicketRequest){

    }*/
}
